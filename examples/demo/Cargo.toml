[package]
name = "example-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
more-config = { path = "../../src", features = ["mem", "env", "cmd", "json", "binder"] }
serde = { version = "1.0", features = ["derive"] }